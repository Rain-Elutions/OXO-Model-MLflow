name: OXO-Model-MLflow

# python_env: ./files/config/python_env.yaml
conda_env: ./files/config/conda.yaml

entry_points:
  main:
    parameters:
      n_estimators: {type: int, default: 7200}
      max_depth: {type: int, default: 5}
      learning_rate: {type: float, default: 0.01}
    command: "python mlflow_test.py {n_estimators} {max_depth} {learning_rate}"
  # validate:
  #   parameters:
  #     data_file: path
  #   command: "python validate.py {data_file}"